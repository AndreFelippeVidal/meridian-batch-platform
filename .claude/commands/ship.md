---
description: Pre-ship checklist — verify the repo meets the portfolio standard before a commit/post
---
Run the ship checklist for this project and report what is missing:
1. make lint && make typecheck && make test  (all green?)
2. README has: one-line value prop, badges, live-demo/write-up links, Mermaid diagram,
   Stack table filled, "What I learned" written (not placeholder).
3. At least one ADR exists in docs/adr/ for the main design choice.
4. A demo artifact exists (docs/demo.gif or a live link).
Then draft a 2-3 sentence commit message. Do NOT push; just report and stage.
